[project]
name = "uniqlo-parser"
version = "0.1.0"
description = "UNIQLO homepage campaign parser"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.50",
]

[tool.uv]
dev-dependencies = []
