[project]
name = "uniqlo-parser"
version = "0.1.0"
description = "UNIQLO homepage campaign parser"
requires-python = ">=3.11"
dependencies = [
    "deep-translator>=1.11.4",
    "playwright>=1.50",
]

[tool.uv]
dev-dependencies = []
